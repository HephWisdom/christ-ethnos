MONGODB_URI=mongodb+srv://<username>:<password>@<cluster-url>/?appName=Cluster0
MONGODB_DB=church-site
PORT=4000
ADMIN_API_KEY=change-this-admin-key
# Comma-separated frontend origins allowed to call the API, for example:
# CORS_ORIGINS=https://your-site.pages.dev,https://your-domain.com
CORS_ORIGINS=
# Optional when the frontend is hosted separately from the API server.
VITE_API_URL=
VITE_WHATSAPP_NUMBER=
